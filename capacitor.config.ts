import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.phonecontrolhub',
  appName: 'Phone Control Hub',
  webDir: 'dist'
};

export default config;
